# 🧴 Online Korean Cosmetics Catalog

Веб-каталог корейской косметики с системой управления товарами.

## ✨ Особенности

- 🛍️ **Каталог товаров** с фильтрацией по брендам, категориям и тегам
- 🔍 **Поиск товаров** по названию и описанию
- 📱 **Адаптивный дизайн** для мобильных устройств
- 🔐 **Админ-панель** для управления товарами
- 🎨 **Интерфейс** с модальными окнами

## 🛠️ Технологии

### Backend
- **Go** (Golang) - серверная часть
- **MySQL** - база данных
- **REST API** - архитектура

### Frontend
- **Vanilla JavaScript** - клиентская логика
- **CSS Grid/Flexbox** - адаптивная верстка
- **HTML5** - семантическая разметка

## 🚀 Быстрый старт

### Предварительные требования
- Go 1.19+
- MySQL 8.0+
- Веб-браузер с поддержкой ES6+

### Установка

1. Клонируйте репозиторий:
```
git clone https://github.com/yourusername/korean-cosmetics-catalog.git
cd korean-cosmetics-catalog
```

2. Настройте базу данных:
```
CREATE DATABASE online_shop;
```

3. Настройте переменные окружения:
```
cp .env.example .env
# Отредактируйте .env с вашими настройками БД
```

Пример .env файла:
```
DB_USERNAME=root
DB_PASSWORD=your_password
DB_HOST=localhost
DB_PORT=3306
DB_NAME=Online_catalog
SERVER_PORT=8080
JWT_SECRET=your_super_secret_jwt_key
```

4.Установите зависимости:
```
go mod download
```

5. Запустите приложение:
```
go run main.go
```

6. Откройте в браузере:
```
http://localhost:8080
```

### 📁 Структура проекта
```
online_shop/
├── internal/
│   ├── auth/         # Аутентификация
│   │   └── middleware.go
│   ├── database/     # Работа с базой данных
│   │   ├── config.go
│   │   └── database.go
│   ├── handlers/     # HTTP обработчики
│   │   ├── auth.go
│   │   ├── handlers.go
│   │   └── products.go
│   └── utils/        # Вспомогательные функции
│       └── errors.go
├── static/
│   ├── css/          # Стили
│   │   └── style.css
│   ├── images/       # Изображения товаров
│   └── js/           # Клиентские скрипты
│       └── script.js
├── templates/        # HTML шаблоны
│   └── index.html
├── .gitignore        # Git ignore файл
├── go.mod           # Go модули
├── init.sql         # Инициализация базы данных
└── main.go          # Точка входа приложения
```

### 🔐 Админ-функции
- Добавление/редактирование/удаление товаров
- Управление наличием товаров
- Добавление тегов и категорий
- Для доступа к админ-панели необходимо войти в систему.

### 🎯 Основные API endpoints
- GET /api/products - список товаров
- GET /api/products/{id} - информация о товаре
- POST /api/products/add - добавление товара (требуется auth)
- POST /api/products/{id}/edit - редактирование товара (требуется auth)
- GET /api/brands - список брендов
- GET /api/category - список категорий
- GET /api/search?q=... - поиск товаров

### 📝 Лицензия
MIT License
